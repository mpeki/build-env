name: build
on:
  push:
    branches:
      - '*'
  pull_request:
    branches:
      - '*'
  workflow_dispatch:      
concurrency:
  group: ${{ github.ref }}
  cancel-in-progress: true
env:
  GITHUB_TOKEN: ${{ secrets.SEMREL_TOKEN }}
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: mpeki/build-env/pre-build@v1
        run: java -version
  release:
    runs-on: ubuntu-latest
    steps:
      - name: Git checkout
        uses: actions/checkout@v3.1.0
        with:
          fetch-depth: '0'
      - name: Prepare semantic-release
        run: |
          npm clean-install
      - name: Do semantic-release
        run: npx semantic-release